autoimmune_mlops:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Path is relative to the directory dbt is invoked from (the dbt/ folder).
      # Matches the repo layout: autoimmune-mlops/duckdb/ and autoimmune-mlops/dbt/
      path: ../duckdb/autoimmune_pipeline.duckdb
      threads: 4
